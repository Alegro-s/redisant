# Docker Hub — даже если входили через Google:
#   https://hub.docker.com → Account Settings → Security → New Access Token
# DOCKER_USER = имя пользователя (не email), например Alegro-s
# DOCKER_PASS = токен (dckr_pat_...), НЕ пароль Google

DOCKER_USER=
DOCKER_PASS=
