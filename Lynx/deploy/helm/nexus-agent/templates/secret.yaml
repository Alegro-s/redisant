{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-agent
type: Opaque
stringData:
  {{ .Values.secret.databaseUrlKey }}: "REPLACE_WITH_POSTGRES_URL"
  {{ .Values.secret.agentKeyKey }}: "REPLACE_WITH_NEXUS_AGENT_KEY"
{{- end }}
